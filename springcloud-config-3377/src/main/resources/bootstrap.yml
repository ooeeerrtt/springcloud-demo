server:
  port: 3377  # 端口号

spring:
  application:
    name: nacos-config-client  # 应用名
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
#        group: TEST_GROUP  #指定分组
#        namespace: 7a0a1a36-88b0-4286-865e-67ef5b7b9e99  #指定命名空间
